DEBUG=false

# ОБЯЗАТЕЛЬНО смените на проде: openssl rand -hex 32
# Дефолтные и короткие (<32 байт) значения отклоняются при DEBUG=false.
JWT_SECRET=change_me_to_a_random_64_char_string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=30

QUEUE_TTL=86400

CORS_ORIGINS=http://localhost:5173
